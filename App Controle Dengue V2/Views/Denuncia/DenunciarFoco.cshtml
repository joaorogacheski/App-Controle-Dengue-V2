@using (Html.BeginForm("DenunciarFoco", "Denuncia", FormMethod.Post, new { @class = "form-horizontal", enctype = "multipart/form-data" }))
{


    <br>
    <fieldset>
        <legend>Denunciar Foco</legend>

        <div id="Endereco" class="form-group">
            @Html.Label("Endereco", "Logradouro", new { @class = "control-label col-sm-2" })
            <div class="col-sm-10">
                @Html.TextBox("txtEndereco", "", new { @class = "form-control" })
            </div>
        </div>

        <div id="Descricao" class="form-group">
            @Html.Label("Descricao", "Bairro", new { @class = "control-label col-sm-2" })
            <div class="col-sm-10">
                @Html.TextBox("txtBairro", "", new { @class = "form-control" })
            </div>
        </div>

        <div id="Descricao" class="form-group">
            @Html.Label("Descricao", "Descricao", new { @class = "control-label col-sm-2" })
            <div class="col-sm-10">
                @Html.TextBox("txtDescricao", "", new { @class = "form-control" })
            </div>
        </div>


        <div id="Doentes" class="form-group">
            @Html.Label("Doentes", "Quantidade aproximada de doentes", new { @class = "control-label col-sm-2" })
            <div class="col-sm-10">
                @Html.TextBox("txtDoentes", "", new { @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <input class="btn btn-primary" type="submit" value="Cadastrar" />
            </div>
        </div>
    </fieldset>


}